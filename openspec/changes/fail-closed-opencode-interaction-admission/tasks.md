## 1. Authorization and predecessor boundary

- [x] 1.1 User confirmed this superseding maximum-permission, zero-approval revision and authorized checkout implementation; live Service/config/session mutation remains outside that authorization.
- [x] 1.2 Confirm `discover-native-harness-routes` is the completed committed exact provider/model/variant baseline; preserve its route tuple and omitted Agent selector without archiving either change in this task.

## 2. Zero-model failing witnesses

- [x] 2.1 Extend the fixed-origin fake OpenCode Server only enough to serve bounded health/provider/config/agent observations, record exact order, and fail on unapproved mutation/model paths.
- [x] 2.2 Add a zero-model RED showing the current code neither installs the exact managed child environment nor sends ordered wildcard allow plus `question`/`plan_exit`/`task` denials.
- [x] 2.3 Add a zero-model RED showing a generic native `ask` is not itself blocking under session wildcard precedence, while absent/non-allow terminal `doom_loop:*` or `duo-workflow-*` remains blocking.

## 3. Maximum-permission session policy and ownership

- [x] 3.1 Install exact child-local `OPENCODE_PERMISSION='{"*":"allow","doom_loop":"allow"}'` for managed children and bind a nonsecret policy-generation digest plus environment identity into the managed-Service fingerprint/receipt without leaking arbitrary environment values.
- [x] 3.2 Create sessions with exact ordered rules wildcard `allow`, `question: deny`, `plan_exit: deny`, `task: deny`, and `doom_loop: allow`; preserve exact provider/model/variant and Agent-selector omission.
- [x] 3.3 Keep attached Servers unchanged: do not restart, replace, or mutate them to install policy, and do not add a broker, prompt tool map, auto-answer, user/global/project config change, or model call.

## 4. Bounded lifecycle witnesses

- [x] 4.1 At list, after managed-Service ensure, and immediately pre-session, revalidate bounded health/provider/config/agent observations; exclude `duo-workflow-*` models.
- [x] 4.2 Resolve exactly one default Agent and require terminal `doom_loop:*:allow` from the final config/agent witness; make final `GET /agent` immediately precede `POST /session`.
- [x] 4.3 On any drift or failed witness, return only sanitized `interactive_policy`, release existing pre-transport claim/lease state, and issue zero session/prompt/model POSTs.
- [x] 4.4 Add stateful fake-Server tests for drift after list, after ensure, and at the final witness; assert all required GET ordering and the zero-POST blocked path.

## 5. Compatibility and focused acceptance

- [x] 5.1 Pin/revalidate the installed `/root/.opencode/bin/opencode` 1.18.23 endpoint and permission-precedence behavior; treat reference source HEAD `e23586af...` and SDK 1.18.18 as non-identical explanatory evidence.
- [x] 5.2 Add focused Driver, managed receipt, fake composed spawn, public/MCP blocker, and diagnostics tests covering ordered policy, doom-loop exception, duo exclusion, attached-server non-mutation, and no Pi/Claude regression.
- [x] 5.3 Sensitivity-check the RED by removing the terminal denial or bypassing final `doom_loop` validation, confirm the intended failure, restore the candidate, and rerun green.
- [x] 5.4 Run focused zero-model tests, `openspec validate fail-closed-opencode-interaction-admission --strict`, and `npm run check`; inspect final diffs for route/Agent-selector drift, config/service mutation, broker/auto-answer/prompt tool map, model call, install, release, archive, or Pi/Claude change.

## 6. Implementation boundary

- [x] 6.1 Checkout implementation is authorized. No live readiness check, Service restart/replacement, config mutation, or session creation is authorized; use deterministic fake-Server evidence only.
