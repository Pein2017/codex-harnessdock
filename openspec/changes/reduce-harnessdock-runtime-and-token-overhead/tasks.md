## 1. Freeze baselines and failure seams

- [x] 1.1 Record the current Linux process topology/PSS, actual eight-tool description characters, eight Skill bytes, Plugin default-prompt characters, and OpenCode v1 envelope overhead in an owner-only acceptance fixture/report; keep RSS and PSS distinct.
- [x] 1.2 Capture and sanitize bounded real `opencode models --verbose` and credential-list diagnostic specimens under the operator's ordinary configuration, then freeze success, malformed, truncated, duplicate, timeout, and unavailable fixtures without a model request.
- [x] 1.3 Add consumer-facing falsification tests that fail on current eager MCP OpenCode startup, two-process bootstrap residency, missing active/unknown turn protection, 13,028-character tool guidance, and 789-character prompt overhead; demonstrate sensitivity at each stable seam before implementation.
- [x] 1.4 Run one disposable read-only Codex-host MCP restart probe. Record whether the same task transparently recovers trusted context and a later operation; stop without implementing MCP idle self-exit unless every contract fact is proven.

## 2. Collapse the MCP bootstrap process

- [x] 2.1 Change the descriptor bootstrap to validate dependencies/environment and dynamically load/await the canonical MCP server in the same Node process; remove child spawn, signal relay, and child-exit plumbing.
- [x] 2.2 Update current and compatibility-shell generation/install checks plus focused MCP/bootstrap tests to require one resident process, canonical loaded source/generation, exact stdio behavior, and actionable missing-checkout/dependency failures.
- [x] 2.3 Verify a launched test MCP exits on stdin close and supported shutdown without leaving a bootstrap/runtime process group or affecting detached durable Agent state.

## 3. Add dormant native OpenCode discovery

- [x] 3.1 Implement one bounded CLI diagnostic reader/parser for native model identifiers, variant efforts, and configured credential providers with exact executable/env/cwd, deadlines, byte/count limits, redaction, and no `--pure`, `--refresh`, session, or model request.
- [x] 3.2 Route absent-Server inspection through fresh dormant discovery, label it distinctly from live validation, and preserve exact dynamic model/effort admission without persisting raw diagnostic/configuration data.
- [x] 3.3 Remove OpenCode ensure from MCP startup, doctor, `list_harnesses`, and zero-model release smoke; retain ensure only in admitted OpenCode pre-transport revalidation.
- [x] 3.4 Revalidate the selected dormant route against fresh live SDK provider discovery after ensure and before native session creation; fail closed on model/effort/auth/catalog drift.
- [x] 3.5 Run focused discovery/Driver/MCP/doctor tests proving non-OpenCode use never starts the Server and a dormant route can cold-start exactly one shared Server before a turn.

## 4. Add owned-only idle lifecycle

- [x] 4.1 Allowlist and validate `HARNESSDOCK_OPENCODE_IDLE_TTL_SECONDS` from the canonical environment file with a 3,600-second default and the design's bounded integer range; expose no public selector or raw value receipt.
- [x] 4.2 Version the managed service receipt with start/last-turn activity timestamps and safe in-fence upgrade behavior for the existing receipt, without treating health/discovery/ensure as activity.
- [x] 4.3 Add owner-only durable OpenCode turn leases before possible native acceptance; release only after proven pre-transport rejection or exact terminal settlement and retain acceptance/settlement-unknown leases across worker exit.
- [x] 4.4 Implement fenced `reapIfIdle` using exact receipt/process/command/loopback checks, held-lease and live-peer refusal, bounded graceful process-tree termination, matching-receipt removal only after confirmed exit, and a minimal owner-only tombstone.
- [x] 4.5 Invoke bounded reclamation at MCP startup and operation completion and schedule at most one unreferenced next-opportunity timer per MCP process; add no detached reaper or permanent supervisor.
- [x] 4.6 Run lifecycle falsification tests for one-hour eligibility, health not refreshing activity, active/unknown lease holds, reused/unowned immunity, identity/command drift, concurrent contenders, ambiguous termination, tombstone bounds, durable-state preservation, and exact cleanup of test process groups.

## 5. Reduce model-visible control text

- [x] 5.1 Compact MCP common instructions and operation descriptions while preserving the exact eight schemas, route fields, authority/topology, fixed wait, completion acknowledgement, and fail-closed semantics; enforce the actual exposed 4,500-character aggregate budget.
- [x] 5.2 Compact the eight lifecycle Skills and Plugin default prompts at their owning layer, keep invocation/stop/error guidance complete, and enforce the 12,000-byte and 800-character aggregate budgets.
- [x] 5.3 Introduce OpenCode prompt envelope v2 at no more than 450 control characters, preserving caller text identity, delimiter refusal, authority, leaf/no-delegation, final-only bounded output, unknown reporting, digest/version lineage, and removing universal citation demands.
- [x] 5.4 Run prompt/Skill/MCP contract tests that prove the size assertions have semantic teeth and that completion messages/delivery tokens remain complete rather than truncated for token accounting.

## 6. Acceptance, release, and lifecycle ordering

- [x] 6.1 Run all focused suites and `npm run check`; verify zero surviving test-owned service/supervisor processes and report process counts plus Linux PSS without treating aggregate RSS as unique memory.
- [x] 6.2 Bump the minor version and changelog, refresh/install the canonical Plugin, and run zero-model release smoke proving one-process MCP startup, dormant/live discovery, exact eight-tool/Skill contracts, context budgets, and no Harness model request.
- [x] 6.3 After zero-model gates pass, run at most one explicitly authorized `openai/gpt-5.6-luna` low-effort OpenCode turn with no automatic retry; report provider input, cache-read, cache-write, output, reasoning, and cost fields separately and verify the requested result.
- [x] 6.4 Start a fresh Codex task and verify the loaded installed Plugin can list dormant routes, cold-start OpenCode on demand, preserve Pi/Claude behavior, and expose the compact guidance; do not require old already-running tasks to shed their existing bootstrap child.
- [x] 6.5 Before archiving this change, obtain authorization to sync or archive completed predecessor `manage-local-opencode-service`, validate this delta against that resulting base, and stop without archive or push unless separately authorized.
