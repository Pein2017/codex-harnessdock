## 0. Dependency Gate

- [x] 0.1 Accept and implement Change A, then freeze one stacked source/spec
  baseline containing the named route/worktree/provenance/catalog/Agent-card/
  blocking/parity changes and its exact MCP generation.
- [x] 0.2 Prove current singular spawn, zero-argument `list_harnesses`, waits,
  target-worktree authority, Agent Cards, blocking, and direct-Harness parity
  remain the baseline B must preserve.

## 1. Failing Typed and Preflight Contracts

- [x] 1.1 Add RED tests for `rows` bounds, strict current spawn-row fields,
  duplicate `task_name`, omitted route fields, forbidden shared/default/retry/
  Team/DAG fields, and rejection of `agent_name` as input.
- [x] 1.2 Add RED tests proving structural failure creates no service, Agent,
  mailbox, job, lease, native session, or model work.
- [x] 1.3 Add RED tests proving environment preflight performs at most the
  existing bounded service ensure, shares discovery only by exact canonical
  `(harness, executionRoot)`, rejects existing root names and same-root batch
  writers, and launches no row if any environment preflight fails.

## 2. Ninth Public Tool, Skill, and Generation

- [x] 2.1 Add `dispatch_agents` as exactly the ninth strict MCP/runtime
  operation and `$codex-harnessdock:dispatch-agents` as the ninth matching
  Skill; retain singular `spawn_agent` as the one-Agent path.
- [x] 2.2 Define the exact ordered input and row receipt, including derived
  `agent_name`, `agent_exists`, five closed outcomes, success-only Agent Card,
  and bounded sanitized stop evidence.
- [x] 2.3 Increment one MCP API generation subsequent to Change A and update
  compatibility/discovery fixtures without changing zero-argument
  `list_harnesses`.

## 3. Stateless Sequential Dispatch

- [x] 3.1 Extract or reuse one internal row-launch seam shared with singular
  spawn; do not copy route, Agent, mailbox, handoff, or recovery ownership.
- [x] 3.2 Implement pure structural and bounded environment preflight without
  durable batch identity or cross-root atomicity claims.
- [x] 3.3 Launch rows sequentially in caller order, repeat authoritative final
  target/name/route/lease/Driver gates, and allow only exact-pair call-local
  discovery reuse.
- [x] 3.4 Continue after `rolled_back` or `lifecycle_owned`, stop after the first
  `ownership_uncertain`, and never retry or roll back an earlier row.

## 4. Cancellation, Lost Response, and Accounting

- [x] 4.1 Check cancellation before each row; if it arrives during a row, obtain
  that row's Change A disposition before marking later rows `not_attempted`.
- [x] 4.2 Prove transport loss does not enable replay, skip-existing, batch
  reconstruction, interruption, or cleanup; reconciliation uses deterministic
  public Agent names and existing operations.
- [x] 4.3 Extend the operator ledger with one batch-call count, N requested route
  rows, and closed row outcomes under Change A privacy/provenance rules; add no
  model-facing or batch provider-usage aggregate.

## 5. Release and Deterministic Acceptance

- [x] 5.1 Update zero-model release smoke and plugin contracts to exactly nine
  tools/Skills and keep the serialized MCP catalog within 4,500 characters
  without deleting decision-bearing wording.
- [x] 5.2 Measure N=1/4/8 fake-runtime calls and wall time. At N=4 and N=8,
  dispatch must remove at least N-1 launch calls and keep discovery flat per
  exact `(harness, executionRoot)` group; otherwise reject B and retain singular
  spawn only.
- [x] 5.3 Inject structural failure, route/readiness failure, writer conflict,
  `rolled_back`, `lifecycle_owned`, `ownership_uncertain`, mid-row cancellation,
  and lost response; any wrong row existence/outcome or later launch after
  uncertainty rejects implementation.
- [x] 5.4 Run focused tests, `npm run check`,
  `openspec validate add-stateless-batch-agent-dispatch --strict`, status, and
  diff-scope checks.
- [x] 5.5 Stop without live Harness/model tests, install, Plugin-cache refresh,
  release, push, archive, main-spec synchronization, or worktree creation unless
  separately authorized.
