## 1. Freeze source-independent matrix fixtures

- [x] 1.1 Begin on the accepted explicit-effort plus provenance-v3 implementation base; do not wait for final Claude discovery acceptance. Define one machine-readable differential matrix fixture and rendered sanitized receipt under `tests/runtime/fixtures/`, with Harness, Driver/capability schema version, dimension, direct source, HarnessDock source, mode, comparator, result, and artifact digest fields. Close `result` to `pass`, regression `fail`, prerequisite `hold`, and capability-derived `not_applicable`; a test that correctly records an unavailable prerequisite as `hold` passes, but final parity/release acceptance requires every required cell to be `pass` or justified `not_applicable`. The separately authorized zero-prompt Agent SDK initialization witness now closes Claude Code 2.1.250 exact dynamic model/effort discovery with a sanitized source-bound receipt; no static catalog/config/authority/transport fallback is used.
- [x] 1.2 Add independent fake-native fixture families for Claude stream-json/control, Pi RPC, and OpenCode Server/HTTP in `tests/runtime/fixtures/`. Direct and Driver sides must not share a HarnessDock-owned expected-value helper or constant; each fixture must remain zero-model-cost.
- [x] 1.3 Add a matrix runner and focused tests under `tests/runtime/` that cover exact model/effort inventory, argv/environment, configuration inheritance, prompt/authority delta, event/tool order, interrupt, `exact_session_continuation`, `cross_process_turn_observation_or_reconciliation`, `automatic_recovery_exact_session_transport`, `same_session_recovery_prompt`, terminal classification, route drift, native usage provenance, process lifecycle, and explicit capability-derived `not_applicable` cells.

## 2. Add only bounded Driver seams

- [x] 2.1 Add test-only or existing-receipt capture seams in `runtime/claude-code-driver.mjs`, `runtime/pi-driver.mjs`, and `runtime/opencode-driver.mjs` for the native facts consumed by matrix rows. Keep `runtime/index.mjs` at its existing eight public lifecycle methods; do not add a generic event/session API, transcript store, ACP layer, or model-facing native selector.
- [x] 2.2 Compare exact equality for route/config inputs and native references, ordered equality for events, closed mapping for terminal outcomes, and source-field correspondence for usage. Preserve unknown/missing as unknown and do not infer prices, costs, or model quality.
- [x] 2.3 Prove `write: true` and `write: false` have byte-equivalent native argv, environment, configuration, tools, plugin/MCP inputs, sandbox inputs, and transport options for every `prompt_only` route; only bounded prompt text and receipts may differ.

## 3. Separate session continuation, old-turn observation, and transport recovery

- [x] 3.1 For each route claiming `exact_resume`, add a fresh-process `exact_session_continuation` fixture that records stable `nativeSessionRef` `S` and accepted `T1`, constructs a fresh Driver/process, passes `S` with new continuation input, and proves distinct provider-native `T2` remains in `S`. Separately, record no-prompt old-turn observation only for `terminal_observable`, exact-session recovery only for `automaticRecovery=exact_session_transport`, and one generated new recovery prompt after reconnect only for `automaticRecovery=same_session_recovery_prompt`. Reject PID/request/job/index/replay/scope-local/synthetic identities and ambiguity. Claude's accepted recovery behavior is the latter, so its exact-session-transport row is `not_applicable`; Pi/OpenCode keep their existing capability-derived rows.
- [x] 3.2 Record OpenCode interrupt, `exact_session_continuation`, `cross_process_turn_observation_or_reconciliation`, `automatic_recovery_exact_session_transport`, and `same_session_recovery_prompt` cells as `not_applicable` only when the accepted capability snapshot says unsupported; do not omit or call them parity.

## 4. Deterministic acceptance and optional live boundary

- [x] 4.1 Sensitivity-check independent-oracle ownership, route/config mutation, event reordering, authority-dependent native input, same-session/wrong-new-turn continuation, wrong-old-turn or ambiguous no-prompt observation, duplicate-input/replayed or unbound automatic recovery, usage-source mismatch, and process cleanup. Each decision-bearing row must fail under its mutation and pass again only after restoration.
- [x] 4.2 Run the matrix-focused suite, relevant existing Driver suites, `npm run check`, and `openspec validate prove-direct-harness-parity --strict`; require zero surviving test-owned processes and source-specific sanitized deterministic receipts.
- [ ] 4.3 Keep any live witness separately authorized: after 4.2, run at most one minimal low-effort turn per freshly advertised Harness, stop without retry/fallback on auth, quota, route, or protocol failure, and store only a sanitized source-bound receipt. Its result gates only its stated live claim, never the deterministic plumbing result.
