# Bind Routes to Native Provenance Implementation Plan

> **For agentic workers:** Use `superpowers:executing-plans` task-by-task; the OpenSpec task list is the tracker.

> **Authority:** OpenSpec change [`bind-routes-to-native-provenance`](../../../openspec/changes/bind-routes-to-native-provenance/) owns scope, semantics, and completion. Execute its `tasks.md`; this file orders the work only.

**Work order**

1. Confirm the accepted shared explicit-canonical-effort foundation; it permits generic schema-v3 and whole-replacement work now.
2. Add task 1 RED fixtures before changing the capability snapshot schema: Pi/OpenCode cover native evidence; Claude under `HOLD` covers only `checkout_declared` plus unavailable safe configuration-generation evidence and rejects unbacked dynamic/session provenance.
3. Implement generic task 2 capability/inspection-contract migration, then task 3 persistence/projection work. Defer universal exact-route revalidation until `enforce-exact-discovered-routes` is accepted, and Claude dynamic/native provenance until successful `discover-claude-native-routes` discovery.
4. Rerun focused Driver, store, MCP, and diagnostic checks after each coherent slice.
5. Perform task 4 sensitivity and checkout acceptance; preserve old-record read-forward behavior. Do not call the final exact-route/provenance stack accepted until the universal exact-route dependency lands.

**Execution boundary:** no inventory, arbitrary config reading, provenance selector, live turn, installation, release, archive, or version change.
