# Enforce Exact Discovered Routes Implementation Plan

> **For agentic workers:** Use `superpowers:executing-plans` task-by-task; the OpenSpec task list is the tracker.

> **Authority:** OpenSpec change [`enforce-exact-discovered-routes`](../../../openspec/changes/enforce-exact-discovered-routes/) owns scope, semantics, and completion. Execute its `tasks.md`; this file orders the work only.

**Work order**

1. Verify the prerequisite `discover-native-harness-routes` base state; stop for an OpenSpec contradiction rather than editing approved artifacts.
2. Add and run the task 1 RED cases at the shared contract and public admission seams.
3. Complete task 2 as one shared route-shape change, then rerun the focused contract/Pi/OpenCode tests.
4. Complete task 3 guidance/authority checks without installing or regenerating an identity shell.
5. Run task 4 sensitivity and deterministic acceptance commands; inspect only the owned diff.

**Execution boundary:** one writer; no source-independent Claude discovery, provenance, parity matrix, live provider turn, installation, release, archive, or version change in this change.
