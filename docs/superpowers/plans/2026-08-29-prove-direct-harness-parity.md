# Prove Direct Harness Parity Implementation Plan

> **For agentic workers:** Use `superpowers:executing-plans` task-by-task; the OpenSpec task list is the tracker.

> **Authority:** OpenSpec change [`prove-direct-harness-parity`](../../../openspec/changes/prove-direct-harness-parity/) owns scope, semantics, and completion. Execute its `tasks.md`; this file orders the work only.

**Work order**

1. Confirm all three prerequisite changes are accepted on one candidate base.
2. Execute task 1 to freeze independent direct-native fixture families and a capability-qualified matrix before Driver changes.
3. Add the narrow task 2 seams and task 3's separate gates: same stable session plus a distinct accepted continuation turn, no-prompt binding of an already accepted old turn, and provider-bounded reconnect recovery without duplicate input/replay; keep the public lifecycle surface unchanged.
4. Run task 4 sensitivity, focused matrix/Driver checks, leak checks, and full checkout validation.
5. Stop after deterministic acceptance unless separate live-witness authorization is granted.

**Execution boundary:** no generic runtime plane, automatic retry, inventory exposure, production-config mutation, installation, release, archive, or model-quality/cost claim.
