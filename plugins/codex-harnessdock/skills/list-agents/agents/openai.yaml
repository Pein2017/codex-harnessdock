interface:
  display_name: "HarnessDock List Agents"
  short_description: "Experimental: list current-root durable Agents"
  default_prompt: "Experimental: call mcp__codex_harnessdock__list_agents for current-root state. Show flat Agent/status/Harness/model/effort/topology/authority rows; omit nulls, timestamps, provenance, tokens, and output. Listing is not progress and cannot reactivate an idle Codex parent. Never use it solely to recheck completion after a quiet wait_agent timeout; call wait_agent again directly. Never fall back to shell."
